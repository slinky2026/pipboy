#NOTE TO SELF!
# this marks the directory as a python package. It's empty on purpose, but tells python that the folder
#should be treated as a module namespace and allow imports like:
#from pypboy.screens.inventory import InventoryScreen
#python wouldn't recognise directory otherwise i think
#actually update: this isnt essential post python 3.3 anymore but ill maybe put import statments in here